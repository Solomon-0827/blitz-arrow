# ==========================================
# Google Cloud Run 部署环境变量示例
# ==========================================
# 这些是在 Cloud Run 部署时需要配置的环境变量
# 可以通过 gcloud CLI 或 Cloud Console 配置
# ==========================================

# ---------------------------------------------
# 【必需】用户端服务 (ppanel-user-web)
# ---------------------------------------------
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_SITE_URL=https://user.yourdomain.com

# ---------------------------------------------
# 【必需】管理端服务 (ppanel-admin-web)  
# ---------------------------------------------
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_SITE_URL=https://admin.yourdomain.com

# ---------------------------------------------
# 【可选】两个服务都适用
# ---------------------------------------------

# 默认语言（默认：zh-CN）
# NEXT_PUBLIC_DEFAULT_LANGUAGE=zh-CN

# 禁用遥测（默认：1）
# NEXT_TELEMETRY_DISABLED=1

# CDN 地址
# NEXT_PUBLIC_CDN_URL=https://cdn.jsdelivr.net

# 联系邮箱
# NEXT_PUBLIC_EMAIL=support@yourdomain.com

# 社交媒体链接
# NEXT_PUBLIC_TELEGRAM_LINK=https://t.me/yourgroup
# NEXT_PUBLIC_DISCORD_LINK=https://discord.gg/yourserver
# NEXT_PUBLIC_GITHUB_LINK=https://github.com/yourorg

# ---------------------------------------------
# 【可选】仅用户端需要
# ---------------------------------------------

# 首页统计数据
# NEXT_PUBLIC_HOME_USER_COUNT=10000
# NEXT_PUBLIC_HOME_SERVER_COUNT=50
# NEXT_PUBLIC_HOME_LOCATION_COUNT=20

# ---------------------------------------------
# 部署命令示例
# ---------------------------------------------

# 用户端部署命令：
# gcloud run deploy ppanel-user-web \
#   --source . \
#   --dockerfile docker/ppanel-user-web/Dockerfile.cloudrun \
#   --region asia-east1 \
#   --allow-unauthenticated \
#   --memory 1Gi \
#   --cpu 1 \
#   --min-instances 0 \
#   --max-instances 10 \
#   --set-env-vars "NEXT_PUBLIC_API_URL=https://api.yourdomain.com" \
#   --set-env-vars "NEXT_PUBLIC_SITE_URL=https://user.yourdomain.com"

# 管理端部署命令：
# gcloud run deploy ppanel-admin-web \
#   --source . \
#   --dockerfile docker/ppanel-admin-web/Dockerfile.cloudrun \
#   --region asia-east1 \
#   --allow-unauthenticated \
#   --memory 1Gi \
#   --cpu 1 \
#   --min-instances 0 \
#   --max-instances 5 \
#   --set-env-vars "NEXT_PUBLIC_API_URL=https://api.yourdomain.com" \
#   --set-env-vars "NEXT_PUBLIC_SITE_URL=https://admin.yourdomain.com"

