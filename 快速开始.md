# ⚡ PPanel 快速开始

## 🎯 一句话总结

在 GCP 虚拟机上克隆代码，运行一个脚本，完成部署。

---

## 📋 部署步骤（5 分钟）

### 1. 连接虚拟机

- 打开 [GCP 控制台](https://console.cloud.google.com/compute/instances)
- 点击虚拟机旁的 **SSH** 按钮

### 2. 初始化环境（仅首次）

```bash
# 创建目录
mkdir -p ~/ppanel && cd ~/ppanel

# 下载初始化脚本
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/deploy/1-setup-vm.sh

# 运行
chmod +x 1-setup-vm.sh
./1-setup-vm.sh
```

**完成后输入 `exit` 重新连接 SSH**

### 3. 克隆并部署

```bash
cd ~
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git blitz-arrow
cd blitz-arrow
./scripts/deploy-from-source.sh
```

⏳ **等待 10-15 分钟** 自动完成：安装依赖 → 构建应用 → 创建镜像 → 启动容器

### 4. 配置防火墙

GCP 控制台 → **VPC 网络** → **防火墙** → **创建防火墙规则**

- 名称：`allow-ppanel`
- 来源：`0.0.0.0/0`
- TCP 端口：`3000,3001`

### 5. 访问应用 🎉

```
Admin: http://YOUR_VM_IP:3000
User:  http://YOUR_VM_IP:3001
```

---

## 🔄 更新应用

```bash
cd ~/blitz-arrow
git pull
./scripts/deploy-from-source.sh
```

---

## 📝 常用命令

```bash
# 查看状态
docker compose -f /tmp/docker-compose-local.yml ps

# 查看日志
docker compose -f /tmp/docker-compose-local.yml logs -f

# 重启
docker compose -f /tmp/docker-compose-local.yml restart

# 停止
docker compose -f /tmp/docker-compose-local.yml down
```

---

## 📚 详细文档

- **完整部署指南**：`DEPLOYMENT.md`
- **环境初始化**：`deploy/1-setup-vm.sh`
- **一键部署脚本**：`scripts/deploy-from-source.sh`

---

就是这么简单！🚀
